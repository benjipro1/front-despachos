services:
  frontend:
    image: ${DOCKERHUB_USERNAME}/front-despachos:latest
    container_name: frontend_despachos
    restart: unless-stopped
    ports:
      - "80:80"

networks:
  default:
    driver: bridge
